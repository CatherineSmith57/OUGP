# exp001_cora_case_study_initial

Purpose: first paper-oriented Cora + GCN case study for OUGP.

Subruns:

- `primary_v2/` - current primary result.
- `initial_v1/` - earlier initial result retained for traceability.

Each subrun contains `command.txt`, per-variant JSON histories, and `cora_summary.csv` / `cora_summary.md`.

Cleaned summary table:

- `../../results/tables/cora_case_study_v2_summary.csv`
- `../../results/tables/cora_case_study_v2_summary.md`
