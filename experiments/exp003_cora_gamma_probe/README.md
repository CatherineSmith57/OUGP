# exp003_cora_gamma_probe

Purpose: probe sensitivity to stronger online-memory and cross-coupling coefficients.

Subruns:

- `gamma_g2/` - high graph/parameter memory gamma and write beta probe.

Cleaned summary table:

- `../../results/tables/cora_gamma_g2_summary.csv`
